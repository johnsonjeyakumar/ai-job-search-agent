"""Application tracking domain package (Phase 8).

Contains the controlled lifecycle vocabulary + state machine
(:mod:`app.application_tracking.status`) and immutable event recording
(:mod:`app.application_tracking.events`). The lifecycle and analytics services
live under ``app/services``.
"""
